# Project Status

## Complete

- Migrated all pages from the live Squarespace site to code: Home, About, `/sap`, `/nokia`, `/simpliifinancial`, `/ssfb` — content, images, and layout matched against the live site.
- Applied a Manrope (headings) / Inter (body) font pairing site-wide via `next/font/google`.
- Light design-system refactor: extracted `SectionLabel`, `PullQuote`, and `CalloutNote`; introduced semantic tokens for color, radius, and content width.
- All 6 routes regression-checked visually after each change.
- Reconciled `NavLink` between Figma and code: extracted the inline nav-link pattern out of `SiteHeader` into a standalone `components/NavLink.tsx` (self-contained — reads the active route via `usePathname` itself), matching the Figma `NavLink` atom's Default/Active variants exactly. `SiteHeader` now composes `NavLink` and no longer needs `"use client"` itself. Visual design and navigation behavior are unchanged; type-checked and all 6 routes regression-checked in-browser (including active-state underlining on `/` and `/about`) with no console errors.
- **AI Lab v1 shipped.** Added a lightweight AI Lab section to Home (`app/page.tsx`) between the Hero and Featured Projects, using the existing `SectionLabel` component plus plain text — no experiment previews/thumbnails, intentionally less visually prominent than the case-study sections below it (no image, smaller copy). Section covers what AI Lab is, a keyword line (Claude Code · MCP · Prototyping · Vibe Coding), one enthusiasm line, and a "Explore AI Lab →" CTA to `/lab`. Added `/lab` (`app/lab/page.tsx`) as an intro + experiment listing, framed as a space for AI-assisted experiments and working prototypes rather than a game gallery. Added a new minimal `ExperimentCard` component (`components/ExperimentCard.tsx`, live/coming-soon states) since none of the existing cards fit a status-driven list without an image. Listed experiments: Fruit Smash (live, links to https://fruit-smash-beige.vercel.app/), Apple Merge (coming soon), Drink Merge (coming soon). `/lab` is reachable via the Home CTA only — not added to the top nav — to keep it secondary to the main case studies. No gradients, glow effects, or generic AI imagery used; reuses existing color/radius/spacing tokens throughout. Type-checked and all 7 routes (`/`, `/about`, `/sap`, `/nokia`, `/simpliifinancial`, `/ssfb`, `/lab`) regression-checked in-browser with no console errors.
- Finalized the Figma library's typography scale (Display/Heading/Body/UI/Meta/Quote styles) and base spacing scale (4–96, plus Medium/Large radius and Content/Default layout width) — see [code-to-Figma mapping report](docs/figma-library-mapping.md). Figma library and code not yet updated to match.
- **Figma design system: v1 complete.** ["Portfolio Design System"](https://www.figma.com/design/chYokhtJIt88qHcDMqLJGx/Portfolio-Design-System), in the Reina's Workshop (Professional) workspace, **is the canonical Figma file going forward.** The earlier Starter-plan file (`JCXgB2CD971EPjsb6ra9Ks`) hit that plan's 20-reads/month MCP cap partway through the build and is superseded — it still holds partial work (Foundations + variables/styles + `SectionLabel`) but should not be used or updated.
  - **Foundations — complete.** Variables: Primitives + Color (6 semantic vars), Spacing (4–96), Radius (Medium/Large), Layout (content-default 672) — all scoped, all with WEB code syntax back to the real CSS custom properties. Text styles: all 12 from the mapping doc. Documented on the Foundations page (Cover, Color, Typography, Spacing/Radius/Layout).
  - **Components — complete.** All 13 components live on the single Components page, one Section per component, ordered by purpose (Layout → Case Study → Shared primitives → Home-only) after a QA pass fixed discoverability/naming: `SectionLabel` (sm/md variants), `PullQuote`, `Testimonial`, `CalloutNote`, `CaseStudyFigure`, `ImageGallery`, `CaseStudyHero`, `CaseStudySection` (Show Challenge/Show Impact boolean properties — presence toggles, not content-count variants), `ProjectHighlight`, `AcademicProjectCard`, `NavLink` (Default/Active variants, matched by `components/NavLink.tsx`), `SiteHeader`, `SiteFooter`. Each screenshot-verified against its `components/` source.
  - **Patterns/Templates — complete.** A "Patterns / Templates" page holds two representative patterns built entirely from existing component instances (no new components, nothing redesigned): **Case Study / Default** (SiteHeader → CaseStudyHero → CaseStudySection → CaseStudyFigure → SiteFooter) and **Home / Project Listing** (SiteHeader → PullQuote intro → 2× ProjectHighlight + 1× AcademicProjectCard → SiteFooter). Both validated visually and structurally (all instances confirmed pointing to their correct main components).
  - Known approximation carried into v1: `CaseStudyHero`'s title uses the Display/Medium text style, since the code's `text-4xl sm:text-5xl` h1 doesn't map 1:1 to any tier in the finalized Heading scale (same unreconciled gap called out below and in the mapping doc — Display/Medium was picked as the nearest match, not a new decision).
- **Home redesigned as a two-column, grid-line layout.** Home (`app/page.tsx`) moved off the narrow centred `max-w-content` column to a full-bleed two-column grid that aligns to the same `px-6 sm:px-10` gutters as `SiteHeader`, so the columns line up with the nav at every width. The columns are roughly 25% / 75% (`1fr` / `3fr` from `xl`, `1fr` / `2.4fr` at `lg` so the sidebar stays readable) and are separated by a single hairline rule — a `border-l` on the taller right column, so the line runs the full height of the page. Scrolling is ordinary page scrolling; neither column scrolls independently.
  - **Left column:** name, role, a one-line specialty, a compact AI Lab blurb + link, and contact links (email, LinkedIn, résumé).
  - **Right column:** a short positioning statement, a "Selected work" rule + count, then SAP, Nokia, the Nokia testimonial, and an "Academic projects" rule followed by Simplii Financial and SSFB.
  - **Project presentation unified.** `AcademicProjectCard` was folded into `ProjectHighlight`, which is now the single project entry used by all four projects: 16:9 image frame, company name, work-focused title, short summary, optional meta line, and a "View case study →" call to action. The long blue underlined headings and 🔗 / 💪🔧🏢📅 emoji were dropped. Only the title is a link; its `::after` overlay makes the whole entry clickable, so each project is one tab stop with descriptive link text and a visible focus ring. Images are `object-contain` inside the fixed frame with `imageBg` matched to each asset's own background (`#eaf8ff` for SAP, white for Nokia, near-black for the two academic projects) — a fixed crop cut the SSFB headline and the Simplii device mockups, so the frame letterboxes instead of cropping.
  - **Content** is reused from the previous homepage and the case studies, only shortened — no new achievements, metrics, or project details. `hero-icon.webp` is no longer referenced by Home (the left column is type-only); the asset is still in `public/images/`.
  - New `EyebrowLabel` component for the small uppercase section labels. `SectionLabel` was deliberately left untouched, since `/lab`, `/simpliifinancial`, and `/ssfb` depend on its current blue heading appearance. New `--color-rule` token (neutral-200) backs every hairline: column rule, list dividers, section rules, and image frames.
- **Home refined: fixed header, sticky-without-slide sidebar, scroll reveals, tightened copy.** Follow-up pass on the two-column redesign above.
  - **Header is now `fixed`, constant height (`h-20`, 80px), with a 1px `border-b border-rule` bottom edge** — the same hairline token as the sidebar's vertical rule, and positioned so the two visually connect into one grid line (see below). No scroll-to-hide, collapse, or shrink behavior was added or existed before; the header simply stays put. `main` carries a global `pt-20` to reserve the header's space on every route, and the `#projects` anchor's `scroll-mt` was raised from `10` to `24` so a hash-link jump clears the fixed header instead of tucking content under it.
  - **Sidebar no longer slides before it sticks.** The left column's outer box now carries no padding/margin of its own, so its rest position — the top of the grid row, right under the fixed header — is exactly `80px`, identical to its `sticky` `top-20` offset. Since the two values match, it's already "stuck" from the very first pixel scrolled instead of sliding up to meet the threshold. (Content padding moved to an inner wrapper so the box position used for the sticky calculation stays untouched.) The `min-height` gate that disables sticky on short viewports (so the sidebar's bottom — the contact links — can never get pinned out of reach) moved from `820px` to `640px`, since the simplified sidebar content (see copy below) is only ~465px tall now, versus ~750px before.
  - **Divider now starts flush with the header.** The right column's `border-l` lives on an outer box with no padding of its own (same trick as the sidebar), so the vertical rule begins at the grid row's top — flush against the header's `border-b` — while an inner wrapper's `pt-10`/`lg:pt-12` pushes the actual text down for breathing room without breaking the line.
  - **Entrance animations, two kinds.** Header and sidebar get a pure-CSS `.animate-fade-in` keyframe (opacity only, 500ms, runs on mount, no JS involved) — see `app/globals.css`. Right-column content uses a new `components/Reveal.tsx`: an IntersectionObserver wrapper toggling `.reveal-up.is-visible` (opacity + an 8px upward shift, 400ms ease-out) the first time an element scrolls into view. It wraps the headline block and each project (image/title/summary revealed together as one group, per `ProjectHighlight` call) — never the "Selected work"/"Academic projects" labels or the `border-t` divider lines, which stay stationary as asked. `prefers-reduced-motion: reduce` disables both animations (instant, final state). Accessibility fallback: a `<noscript>` rule in `app/layout.tsx` forces every `.reveal-up` back to visible when JS is disabled (an early approach that toggled an `html.js` class from an inline script was dropped — mutating a React-hydrated element's className caused a hydration mismatch); `Reveal` itself fails open (visible immediately) if `IntersectionObserver` is unsupported or its setup throws.
  - **Copy tightened, one unverified claim removed.** Sidebar: role stays "UX Designer"; the previous descriptive paragraph and 5-item Specialties list were replaced by a single "Specialty" line, "Design systems & product design" (the SAP/Nokia specifics that paragraph carried now live only in the new main-column supporting sentence, so nothing is duplicated); AI Lab's blurb is now "Exploring AI-assisted design, prototyping, and creative coding." Main column: the headline is now "Designing clearer experiences for complex products." followed by a separate supporting sentence, "At SAP and Nokia, I worked on shared components and interaction patterns for complex enterprise workflows." The Simplii Financial summary no longer claims "increased user engagement" — no metric or research backs that outcome anywhere in the case study, so the sentence now ends at "clear information delivery." All other project summaries, meta lines, and case-study pages are unchanged.
  - Verified at 1920/1440/1280/1024 wide (two columns; sticky engages with no slide at both tall and newly-lowered short viewports, confirmed via computed `getBoundingClientRect()` snapshots across scroll positions), 768 and 390 wide (single column, header still fixed and visible, sidebar static and flowing above projects), the `min-height:640px` sticky gate on/off either side of the threshold, the `/#projects` hash link against the fixed header, keyboard tab order and focus rings, all four project links, no console/hydration errors, and `next build`. `/about`, `/lab`, and the case-study pages were re-checked against the new global fixed-header spacing and are visually unchanged. `prefers-reduced-motion` was verified by code review of the CSS (no override toggle was available in the browser tooling used this session).
- **Home: two-column project grid, image hover treatment, nav/sidebar simplification.** Two combined passes — project layout/hover, then navigation, sidebar, and copy — on top of the two changes above.
  - **Projects now sit in a two-up grid.** SAP and Nokia are one row under "Selected work"; Simplii Financial and SSFB are one row under "Academic projects" (order unchanged). Each grid uses a shared 32px gap (Tailwind's `gap-8`) and `grid-cols-1 @min-[752px]:grid-cols-2`, so it drops to one column once a card would fall under ~360px — checked against the actual rendered width of the right column itself via a Tailwind v4 container query (`@container` on the right column's content wrapper), not the viewport, since the sidebar's own width isn't a fixed fraction. 752px = 2 × 360px card + the 32px gap; the exact wording lives in a `PROJECT_GRID` class-string constant in `app/page.tsx` (Tailwind's class scanner needs a literal string, so this can't be computed from a variable at build time). The per-card `border-t` dividers between stacked projects were removed now that SAP/Nokia and Simplii/SSFB sit side by side — the gap alone separates them; the section-level dividers above "Selected work" and "Academic projects", and the one below the Nokia testimonial, are unchanged. Thumbnails moved from 16:9 to 16:10.
  - **`ProjectHighlight` reordered: company name and title now sit above the thumbnail**, so the work is identifiable before the (often large or slow-loading) image resolves. Image, summary, meta, and CTA follow in the same order as before.
  - **Image hover/focus effect.** The frame (`overflow-hidden`, `border border-rule`) stays completely stationary — only the `<Image>` inside it transforms, so it can never fight the `Reveal` entrance animation applied to the whole card by the call site. At rest: `scale(1.04) brightness(1)`. Hovering or focusing anywhere in the card — `group-hover`/`group-focus-within` on the same `group` `<article>` the `::after` link overlay already uses — settles it to `scale(1) brightness(.94)` over Tailwind's default `transition` (which already includes `transform`/`scale`/`filter`) at `duration-500 ease-out`, reversing the same way on exit. `motion-reduce:transition-none` drops the animated tween under `prefers-reduced-motion` while leaving the two end states themselves intact (an instant, non-animated swap, which is the same "keep the states, remove the animation" pattern already used for `.reveal-up`/`.animate-fade-in`). No separate touch-device handling was needed: the card's link overlay already makes the whole thing tappable regardless of whether a given touch browser ever triggers `:hover`, since the hover styling is purely decorative and gates no content. One thing worth knowing: Tailwind v4 compiles `scale-[…]`/`scale-100` to the standalone CSS `scale` property, not a `transform: scale(...)` value — `getComputedStyle(el).transform` reads `"none"` even when the effect is fully applied; check `.scale` instead.
  - **Navigation simplified.** `SiteHeader`'s nav is now "Work · About · Resume ↗" — "Home" was removed (the logo already links to `/`) and "Projects" was renamed to "Work" (still linking to `/#projects`).
  - **Sidebar rewritten as a personal intro.** The "Specialty" eyebrow heading is gone; identity is now one block — "Reina Kim" / "UX Designer · Vancouver" / "Design systems & product design" — with no sub-heading breaking it up. AI Lab keeps its own "AI Lab" eyebrow heading and link, copy now "Turning small ideas into working prototypes with AI." Contact is down to email + LinkedIn (Resume was dropped as a duplicate now that it's a persistent nav item) and restyled quieter: `text-muted`/`text-sm`, no default underline, `hover:text-ink hover:underline`, no more "Contact" eyebrow heading — spacing between the three blocks (identity, AI Lab, contact) is `gap-8` so they still read as distinct groups without labels doing that work.
  - **Copy.** The main headline is unchanged ("Designing clearer experiences for complex products."); the supporting sentence is now "At SAP and Nokia, I designed reusable components and aligned interaction patterns across enterprise products."
  - Verified: the `@container`/`@min-[752px]:grid-cols-2` rule against computed `gridTemplateColumns` and per-card widths across a range of container widths (confirmed it correctly drops to one column in the 1024–1280px range, where the outer sidebar/right-column ratio itself briefly narrows the right column below 752px — an accurate reaction to the existing outer-grid breakpoint, not a defect); the hover/focus effect via computed `.scale`/`.filter` before, during, and after real hover and programmatic focus (confirmed reversal after the 500ms transition); `motion-reduce:transition-none` compiles under the correct media query; all nav and project links; 1440/390 wide layouts; and `next build`/lint/typecheck. `prefers-reduced-motion` was otherwise verified by code review (no override toggle was available in the browser tooling used this session).
- **Nav gains AI Lab; page shell extracted; About rebuilt on it with personal copy.** Nav is now "Work · About · AI Lab · Resume ↗" everywhere (`SiteHeader`'s `NAV_LINKS` gained `{ href: "/lab", label: "AI Lab" }`); the Home sidebar's dedicated AI Lab block (heading, blurb, "Explore AI Lab →") was removed now that it's a persistent nav item, leaving the sidebar identity + contact only, as asked.
  - **New `components/TwoColumnLayout.tsx`** holds the exact shell Home's redesign established — the `px-6/sm:px-10` outer padding, the `lg:grid-cols-[1fr_2.4fr]`/`xl:grid-cols-[1fr_3fr]` column ratio, the `border-l` that starts flush with the header, the `pt-10/lg:pt-12` content offset, the `animate-fade-in` sidebar, and the sticky-without-slide `top-20` mechanic — parameterized as `left`/`right` render props. Home was refactored onto it with no visual change (verified by re-screenshotting and re-measuring header/divider offsets). The one thing that can't be shared as a single value: the `min-height` sticky-gate query, since it has to match each page's own sidebar height or a short viewport could pin a sidebar whose bottom (the contact links) never scrolls into view. It's a `stickyGate` prop selecting between two full literal class strings kept in the same file (`STICKY_GATE.default`/`.tall` — Tailwind's scanner needs literal text, so a value built from a prop or template string wouldn't be picked up); Home uses the default (640px, calibrated for its ~465px-tall sidebar), About passes `"tall"` (760px, calibrated below).
  - **New `components/ContactLinks.tsx`** factors out the email + LinkedIn list (and its quiet styling) that both Home's and About's sidebars now render identically.
  - **`/about` rebuilt on `TwoColumnLayout`.** Left column: "About" heading, the existing `about-photo.webp` portrait (kept its established arch `rounded-t-full` treatment, just moved into the narrower sidebar column and width-capped at `15rem` — uncapped, a `w-full` image would grow taller on wider viewports independent of viewport *height*, which would have made a single min-height sticky threshold unreliable), then `ContactLinks`. Right column: a short intro (reused exactly as supplied), an "Outside of work" section with four brief, standalone items (gaming, singing/recording, a dog, building small games with AI — none framed as a professional strength, none inventing an anecdote or detail beyond what was given), and a "Background" section with dated, factual work history (SAP internship, May 2026; Nokia co-op, December 2024; earlier credit-union customer service) plus a "View Resume ↗" link. Section rhythm (divider + `EyebrowLabel`, `Reveal`-wrapped content, stationary dividers) matches Home's exactly. The reference screenshot's three-column stats/awards/services/software-catalog/FAQ content was not used — only its thin-divider styling, typography, and portrait-in-the-narrow-column placement carried over.
  - **Known gap:** the "Outside of work" section is text-only for now — no photos exist yet for gaming, singing/recording, the dog, or the AI game-building, and none were supplied this session, so none were fabricated. Each of the four items is a natural slot to drop a small photo into once supplied (see Next steps).
  - Verified: nav renders "Work · About · AI Lab · Resume ↗" and highlights correctly on `/`, `/about`, and `/lab`; Home's header/divider offsets are pixel-identical before and after the `TwoColumnLayout` refactor; About's sidebar height is stable (~515px) across a wide range of *widths* (1440–2600px, confirming the portrait cap works) and its sticky gate engages/disengages exactly at the 760px threshold; mobile (390px) stacks correctly on both pages with the header staying fixed; all nav/contact/project links resolve; no console errors; and `next build`/lint/typecheck.
- **New Nokia homepage thumbnail — heading cropped out, screens enlarged.** New `public/images/nokia-thumbnail.webp`, used only by the Home project card (`app/page.tsx`); `app/nokia/page.tsx`'s case-study hero still points at the original, untouched `nokia-hero.webp`, so the two are now independent assets on purpose.
  - **Made by cropping the existing `nokia-hero.webp` pixels only** — no regenerated text, charts, map, or screen content. Measured the source image programmatically (`PIL`) to find the real content bounds: the "NOKIA Design System" wordmark + heading sat at y 99–126 of the 1139×667 source, and the two overlapping screens' combined bounding box was x 198–927, y 234–666 — note `666` is the source's last row, confirming the light "User Hub" screen was already truncated at the original canvas edge rather than fully depicted, so cropping to that same bottom edge preserves that "continues past the frame" look exactly rather than inventing a hard stop. Cropped to (183, 193, 942, 667) — small ~15px breathing margin on the sides, the heading and its whitespace fully excluded above, no margin added at the bottom — giving a 759×474 image at aspect 1.601, matching `ProjectHighlight`'s 16:10 frame almost exactly, so the screens now fill the card with only a hairline margin instead of being letterboxed under a large empty heading area. Saved as WebP (quality 92).
  - **Nothing about the frame, hover effect, or component changed.** Still passes through the same `ProjectHighlight` (`imageBg="bg-white"`, `object-contain`, the `group-hover`/`group-focus-within` zoom-out + dim treatment) — confirmed via computed `.scale`/`.filter` that hovering the new thumbnail still animates `scale(1.04)→scale(1)` and `brightness(1)→brightness(.94)` exactly as on every other project card.
  - Verified side-by-side against the SAP card at 1440px (both thumbnails now read at a comparable visual scale, each with a small, restrained margin and no exposed background padding) and on a 390px mobile column (Nokia's two screens stay legible at that size); confirmed `/nokia`'s own hero image is untouched; hover/focus effect re-verified on the new asset; and `next build`/lint/typecheck.
- **SAP image replaced everywhere — brand mark swapped for an actual dashboard mockup.** New `public/images/sap-dashboard.webp` (a "SAP Analytics Cloud Example based on Fiori Light Theme" mockup, converted from a supplied PNG at its native 1653×1122) now replaces `sap-hero.webp` in **both** places it was used — Home's project card (`app/page.tsx`) and the `/sap` case-study hero (`app/sap/page.tsx`'s `CaseStudyFigure`) — since, unlike the Nokia change above, the same image was wanted in both spots. The old `sap-hero.webp` file is left in place but is no longer referenced anywhere; nothing else pointed to it.
  - **No cropping needed.** The new image was already a clean, tightly-composed single dashboard (no separate heading element to remove, unlike the original Nokia source), so it's used as-is rather than cropped. `imageBg` on both usages became `bg-[#f4f6f7]`, sampled from the image's own light background, so `ProjectHighlight`'s `object-contain` letterboxing on Home (the image's 1.473 aspect is narrower than the 16:10 frame) blends in rather than showing a mismatched color band. On `/sap`, `CaseStudyFigure` uses `object-cover`, so its `width`/`height` props were updated to the image's real 1653×1122 (matching aspect exactly) — that box always exactly matches the image's own shape, so `object-cover` shows the whole thing with no cropping.
  - **Content fits the page's existing framing.** The `/sap` page's own `CalloutNote` right below the figure already explains that "some details and visuals have been simplified or replaced with public examples due to NDA restrictions" — this image is exactly that: a labeled public example, not real internal SAP work, so alt text on both usages reads "SAP Analytics Cloud example dashboard using the Fiori Light theme" rather than implying otherwise.
  - Verified: the new thumbnail renders and hovers correctly on Home (same `.scale`/`.filter` zoom-and-dim check as Nokia), the full dashboard displays uncropped on `/sap`, both pages checked at 1440px and on a 390px mobile column, and `next build`/lint/typecheck.
- **All four case studies rebuilt on the Home/About two-column shell.** `/sap`, `/nokia`, `/simpliifinancial`, and `/ssfb` now use `TwoColumnLayout` instead of a centered `mx-auto max-w-content` article column — same header, column widths, divider position, padding, and content-start position as Home/About, with no page-specific layout left.
  - **New `components/CaseStudySidebar.tsx`** is the left column on every case study: the project/company name and its existing role (for SAP, literally "SAP" + "UX Design Intern," split out of the old combined title, exactly as asked; the others follow the same pattern from their own existing text — Nokia keeps its full original title "Nokia Design System" since it had no natural name/role split, paired with "Junior UX Designer," quoted directly from its own intro paragraph rather than invented; Simplii and SSFB split their existing two-part titles the same way SAP's did), the existing `meta` facts (dates, tools, team, client — restyled from the old emoji+dt/dd treatment to plain label/value pairs matching Home/About's quieter language), and a **quiet "On this page" table of contents** built from each page's own section headings and anchors (`CaseStudySection` gained an optional `id` prop; ad hoc `<section>` blocks got matching `id`s and `scroll-mt-24` by hand).
  - **`CaseStudyHero` is gone** — every page's `subtitle` (the old blue line above the title) is now the right column's lead headline, styled like Home's positioning statement, immediately followed by the page's existing intro paragraphs and its case-study image, all inside one `Reveal` group, matching "headline, introduction, and image near the top." Everything that followed in the old page (`CalloutNote`, every `CaseStudySection`, ad hoc `<section>`s, `PullQuote`, `ImageGallery`, standalone `Image`s) stayed in exactly its original order, each wrapped in its own `Reveal` with a `border-t border-rule pt-10` divider before it — the same section rhythm as Home — while the "Selecting the client" 2-column text+image sub-layout, Challenge/Impact subsections, and every other internal structural choice were left untouched.
  - **Paragraphs capped at `max-w-prose` (~65ch), images left full-width.** `CaseStudySection` and `CalloutNote` now wrap their paragraph blocks in `max-w-prose`; every ad hoc paragraph block across the four pages got the same class by hand. `CaseStudyFigure`, `ImageGallery`, and standalone `Image`s were left exactly as they were (`w-full`), since only prose — not screenshots or diagrams — was asked to narrow.
  - **Sticky-without-slide, calibrated per page.** Reused `TwoColumnLayout`'s existing `stickyGate` mechanism rather than inventing a new one: SAP (~444px sidebar) and Nokia (~416px) fit under Home's existing `640px` gate, SSFB (~592px) fits under About's existing `760px` gate, and only Simplii Financial — the longest case study, with a ten-item TOC — needed a new `simplii` variant (`880px`, sidebar ~704px). No sidebar content needed its own scroll pane on any of them.
  - **Nothing factual changed.** Every paragraph, image, caption, and contribution claim is byte-for-byte the same text as before, just repositioned into the new shell; only the SAP/Nokia/Simplii/SSFB titles were *split* (not reworded) into name + role, and section headings were reused verbatim as their own TOC labels.
  - **`CaseStudyHero.tsx` deleted** — confirmed unused anywhere else first.
  - Verified: header/divider offsets are pixel-identical to Home/About's `y=80` on every case study; each page's TOC anchors scroll to `y≈96` (clearing the fixed header with a 16px gap) via a genuine DOM `.click()` (a synthetic click from the browser-automation tool itself proved unreliable for same-page hash navigation in this environment — a tooling quirk, not a site bug, confirmed by cross-checking with a real `element.click()` and a real keyboard-driven anchor click); Simplii Financial's full sidebar + 10-link TOC stacks correctly above its content on a 390px mobile column with the header still fixed; and `next build`/lint/typecheck.
- **Fixed a real click-target bug on the homepage project cards, and hardened the pattern.** `ProjectHighlight`'s company name, title, *and* image now all live inside one real `<Link>` (previously only the title text was inside the anchor; the image was a sibling, with the full-card hit area coming entirely from the anchor's `::after` overlay). This is still a single semantic anchor — the `::after` overlay technique still extends the *hit area* to the whole card, background and padding included, covering the summary/meta/CTA text that stays outside the link — so there's no nested link and no JS click handler anywhere.
  - Verified systematically across all four cards: `elementFromPoint` at each card's top-left corner, bottom-right corner, image center, and CTA text all resolve to the correct anchor and `href`; a real click via the browser-automation tool navigates correctly; real Tab-key keyboard focus lands on the link with a visible `outline` ring; and the image's `group-hover`/`group-focus-within` zoom-and-dim effect still fires correctly on both real hover and real keyboard focus (confirmed via `getBoundingClientRect`-based checks after this session hit and diagnosed two testing-tool artifacts along the way — `getComputedStyle` returning stale values for an element under a live `requestAnimationFrame` loop or mid-CSS-transition, and the automation tool's coordinate space not matching `window.innerWidth` — neither of which was a real site bug once cross-checked against real layout measurements and real keyboard input).
- **New shared custom cursor.** `components/CustomCursor.tsx`, mounted once in `app/layout.tsx`: a solid 10px `--color-accent` circle with a ~70ms trailing lag, growing smoothly to 14px over links, enabled buttons, `[role="button"]`, and `summary`. Position updates are written directly to the DOM (a ref, not React state) inside a `requestAnimationFrame` loop reading a plain mutable ref for the latest pointer position, so pointer movement never triggers a React render; the trailing feel itself comes from a CSS `transition: transform 70ms linear` on the positioning wrapper, not manual easing math. `pointer-events: none` and `aria-hidden="true"` keep it fully decorative — confirmed clicks and keyboard activation on project cards are unaffected.
  - **Bails out entirely** — no DOM, no listeners, native cursor untouched — for `(pointer: coarse)` (touch devices) and `prefers-reduced-motion: reduce`, checked once on mount.
  - **Native cursor hidden narrowly, not globally.** `body.cc-cursor-active` sets `cursor: none` (inherited, so plain elements lose their cursor), plus explicit `cursor: none` overrides on `a`/enabled `button`/`[role=button]`/`summary` (needed because those already get an explicit non-inherited `cursor: pointer` from the browser's own UA stylesheet, which would otherwise beat the inherited rule) — and an explicit `cursor: auto` restore on `input`/`textarea`/`select`, so native text-editing and resize cursors are never touched.
  - **Hides on window leave, snaps (doesn't fly) back on re-entry**: `mouseleave`/`mouseenter` on `documentElement` toggle opacity; on both the very first pointer move and re-entry, the wrapper's `transition` is briefly disabled, the position is set, a reflow is forced, then the transition is restored — so the dot never visibly travels from a stale position.
  - **The native cursor is only hidden once the dot is actually positioned and visible** — `cc-cursor-active` is added on the *first* `mousemove`, not on mount, so there's never a frame with no cursor at all.
  - Verified: the dot is visible and correctly colored/sized at rest, grows on hovering a project card and a nav link, reverts on mouseout, doesn't block clicking a project card (confirmed navigation still works with the cursor active) or real keyboard Tab activation, and console is clean. `prefers-reduced-motion` and touch bail-out were verified by code review (matching the same media-query pattern already used elsewhere in the codebase); no touch device or OS-level reduced-motion toggle was available in this session's browser tooling to test live.
- **SAP and Nokia homepage thumbnails simplified to minimal brand covers**, replacing the screenshot-style images from the two changes above (Home only — the `/sap` and `/nokia` case-study pages are untouched, still showing `sap-dashboard.webp` and `nokia-hero.webp`).
  - **New `public/images/sap-cover.webp` and `nokia-cover.webp`**: each project's actual existing logo mark, isolated from an existing screenshot and recomposed onto a flat 16:10 background — SAP's triangle wordmark cropped out of `sap-hero.webp` (2500×1042 source, so a crisp result) with its grid-line/angle-label annotations masked out by color (the mark is a right-triangle, not a rectangle, so a plain bounding-box crop couldn't exclude the annotations in its empty corner without also cutting into the logo); Nokia's wordmark isolated the same way from a small blue instance already present in `nokia-accessibility.webp` (no standalone Nokia logo file exists anywhere in the repo, and none was supplied this session — this is the only existing usable source, at a native ~70×23px, upscaled with `LANCZOS` + a mild unsharp mask). **Known limitation:** the Nokia mark is visibly softer than SAP's up close, purely a source-resolution ceiling — if a proper Nokia logo (SVG or a larger PNG) is ever supplied, `nokia-cover.webp` should be rebuilt from it rather than this low-res crop.
  - **Flat by construction, no added effects.** Each cover is a solid background (`#EEF4FA` SAP, `#F4F5F7` Nokia) with only the logo mark composited on top — no screenshot content, no extra text, no decorative shapes, no gradient or shadow added by this pass (the SAP mark's own built-in blue-to-cyan shading is part of the existing logo artwork itself, not something added here). Each logo starts at 30% of the 1200×750 canvas width, then nudged for optical balance — SAP down slightly to 32%, Nokia's thinner strokes left at 30% rather than sized up further, since enlarging a low-resolution source only would have made its softness more visible.
  - **Project-heading alignment, new for all four homepage cards.** `ProjectHighlight`'s heading block (company + title) now carries `@min-[752px]:min-h-[104px]` — sized to fit the longest of the four titles at two lines — so both cards' thumbnails start at the same y in a row regardless of whether a given title wraps to one line or two; below that breakpoint (mobile, single column) there's no min-height at all, so the heading returns to natural flow. This is a shared, breakpoint-scoped rule, not a one-off fix, so it also applies to the Simplii/SSFB row.
  - Everything else about the cards is unchanged: still one full-card `<Link>`, still `object-contain` inside the 16:10 frame with a matching `imageBg`, still the same `scale(1.04)→scale(1)`/`brightness(1)→brightness(.94)` hover-and-focus treatment (verified again on the new images), still `motion-reduce:transition-none`.
  - Verified: both new covers render correctly with generous whitespace and no visible artifacts at normal display size; heading/thumbnail top-edge alignment holds with SAP's one-line and Nokia's two-line titles side by side, and the alignment rule correctly drops away when the grid stacks to one column at 390px; hover/focus dimming re-confirmed on the new images; Simplii/SSFB's images and copy are unchanged; and `next build`/lint/typecheck. The old `nokia-thumbnail.webp` (the previous Home-only crop, now fully superseded and unreferenced) was deleted; `sap-hero.webp` (already unused before this session) was left alone.
- **Real logo assets, blurred SAP background, native cursor restored, homepage cards de-coupled into two links, left column narrowed ~10%.** Combined pass, four independent changes plus one architecture-verification note:
  - **`sap-cover.webp` and `nokia-cover.webp` rebuilt from genuine transparent-background logo files** (supplied this session — a clean SAP wordmark WEBP and a Nokia wordmark PNG), replacing the earlier color-masked screenshot-crop extractions. Both are simple alpha-composites onto the existing flat canvases (`#EEF4FA` SAP, `#F4F5F7` Nokia) at the same ~30–32%-of-canvas-width sizing as before — no masking artifacts, no softness ceiling; the "get a real Nokia logo" item in Next steps below is resolved.
  - **`sap-cover.webp` also gained a blurred, low-opacity SAP Analytics Cloud dashboard texture behind the logo**, per request: `sap-dashboard.webp` (the same asset already used on `/sap`) is scaled to 1.6× the 1200×750 canvas width, center-cropped back down to canvas size (so it's intentionally oversized and cropped at the edges, never letterboxed), blurred (`GaussianBlur`, radius 22), then blended over the flat background at 22% opacity before the crisp logo is composited on top. The effect is deliberately subtle at the card's rendered display size, more visible zoomed in.
  - **`components/CustomCursor.tsx` / `app/globals.css`: the native OS cursor is visible everywhere again.** Removed the `body.cc-cursor-active`/`cursor: none` rules entirely (previously hidden the native cursor and simulated it with the dot) — the accent-blue dot is now a purely decorative follower alongside the always-visible native cursor, not a replacement for it. Rest state is a solid 10px fill; hovering or keyboard-focusing an actual link or enabled control (same `INTERACTIVE_SELECTOR`, unchanged) grows it to a 40px ring — transparent fill, 1px stroke — over a 180ms ease transition on each property. The stroke's `border-width` only ever animates between the literal `0` and `1px` (never scaled by the growing diameter, via `box-sizing: border-box`), so it reads as a constant 1px once active regardless of size. `pointer-events: none`/`aria-hidden` and the coarse-pointer/reduced-motion bail-out are unchanged.
  - **`components/ProjectHighlight.tsx`: the single full-card link is gone.** The company name, title, description, meta, and surrounding whitespace are now plain, non-interactive text. Only two things are links, as separate siblings pointing at the same `href`: the thumbnail image frame (`aria-label="{company} — {title}"`) and the "View case study" CTA below the description (`aria-label="View case study: {title}"`, added beneath the summary/meta — it already existed as a plain paragraph, now promoted to its own link). Each has its own `focus-visible` outline. The hover/focus zoom-and-dim treatment on the image now triggers only via `group-hover`/`group-focus-visible` scoped to the image link itself, not the whole card. The now-unused `imageAlt` prop was removed from `ProjectHighlight` and all four call sites in `app/page.tsx` (the image is `alt=""`, decorative, since its link already carries a descriptive label).
  - **Shared left column narrowed ~10%.** `TwoColumnLayout`'s grid-template token changed from `1fr`/`2.4fr` (`lg`) and `1fr`/`3fr` (`xl`) to `0.87fr`/`2.4fr` and `0.87fr`/`3fr` — solved so the left column's *share* of the row drops by 10% (≈29%→27% at `lg`, ≈25%→22% at `xl`) while the right column's absolute width is unchanged, since only the left fraction moved. One shared token, so Home, About, and all four case studies stay aligned on the same first-divider position automatically. Paragraph max-widths, typography, and padding are untouched.
  - **Case-study sidebar spacing confirmed already correct — no code change needed.** `CaseStudySidebar` renders project info and the "On this page" section nav as siblings with no layout of their own; their 32px separation comes entirely from `TwoColumnLayout`'s parent `flex flex-col gap-8` (32px), confirmed via `getBoundingClientRect()` on `/sap` (`nav.top - dl.bottom === 32`).
  - **Note on the requested "case-study right sidebar" / "middle column" / "image lightbox":** the actual site has never had a three-column case-study layout — `TwoColumnLayout` is the single shared two-column shell used by Home, About, and all four case studies alike, with section navigation already living in the one left sidebar (via `CaseStudySidebar`) and no separate right sidebar or lightbox feature anywhere in the codebase (`ImageGallery`/`CaseStudyFigure` are plain, non-interactive images). Nothing needed removing from a right sidebar since none exists, and no lightbox was built, since the instructions asked to *preserve* one rather than clearly commissioning a new feature. Flagging this now in case a lightbox or a genuine right-sidebar split is wanted as new work.
  - Verified: `tsc --noEmit`, `eslint`, and `next build` all clean; both new cover images render correctly at the homepage's display size; each homepage card exposes exactly two links (image + CTA) with correct distinct `href`s and `aria-label`s, and the title/summary text is confirmed outside both links via `closest('a')`; the cursor's 40px ring renders correctly over an actual link (confirmed visually mid-interaction); the left-column narrowing renders correctly on Home and is verified at exactly 32px on `/sap`'s sidebar gap; the `#ai-mcp-workshop` TOC anchor on `/sap` clears the fixed header (lands at `y≈96`, an unchanged 16px margin past the 80px header) after the width change. Mobile/responsive layouts were not re-verified live this pass (only the `lg:`/`xl:` grid fractions changed; the `grid-cols-1` mobile fallback is untouched) due to the session's browser-automation tool hitting its usage limit partway through verification — worth a live spot-check next session.

## Current structure

**Components** (`components/`)
- Layout: `SiteHeader`, `SiteFooter`, `NavLink`, `TwoColumnLayout` (shared by Home, About, and all four case studies), `CustomCursor`
- Case study: `CaseStudySidebar`, `CaseStudySection`, `CaseStudyFigure`, `ImageGallery`
- Shared primitives: `SectionLabel`, `EyebrowLabel`, `PullQuote`, `CalloutNote`, `Reveal`, `ContactLinks`
- Home-only: `ProjectHighlight`, `Testimonial`
- AI Lab-only: `ExperimentCard`

**Tokens** (`app/globals.css`)
- Color: `--color-ink`, `--color-body`, `--color-muted`, `--color-accent`, `--color-rule`
- Radius: `--radius-card`, `--radius-tile`
- Layout: `--container-content`
- Font: `--font-heading` (Manrope), `--font-sans` (Inter)
- Animation: `.animate-fade-in` (opacity-only, load), `.reveal-up`/`.is-visible` (opacity + 8px shift, on scroll into view via `Reveal`) — both respect `prefers-reduced-motion`
- Cursor: `.cc-cursor`/`.cc-cursor-dot`/`.cc-cursor-dot--active` (see `CustomCursor`) — a decorative follower alongside the always-visible native cursor, not a replacement for it; bails out for `prefers-reduced-motion` and coarse pointers

**Known gaps** (not yet formalized): H1/H2 sizes in code not yet reconciled with the finalized Heading scale, no `Surface/Tint-*` background tokens, no breakpoint token — see [code-to-Figma mapping report](docs/figma-library-mapping.md).

## Next steps

1. GIF/video performance optimization
2. Reconcile remaining code/Figma divergence: H1/H2 sizes vs. the finalized Heading scale, and `Quote/Testimonial` (Figma is Manrope 600, code is currently Inter semibold)
3. Build out real experiments (Apple Merge, Drink Merge) behind the `/lab` "Coming soon" cards; add `ExperimentCard` and the AI Lab section to the Figma library once content stabilizes
4. Reconcile the Figma library with the Home/About redesign: `ProjectHighlight` needs rebuilding against its current structure (company/title above a 16:10 image, two-up grid, hover treatment), `AcademicProjectCard` should be retired (it no longer exists in code), `EyebrowLabel`/`Reveal`/`ContactLinks`/`TwoColumnLayout`/the `--color-rule` token need adding, and the Home pattern needs rebuilding as the current layout: fixed header with "Work · About · AI Lab · Resume ↗" nav, a plain personal-intro sidebar (no section headings beyond AI Lab, which no longer even appears on Home), and a two-column project grid; About needs building fresh as a second pattern on the same shell
5. `SiteHeader` becoming `fixed` is global (every route reserves `pt-20` in `app/layout.tsx`), so any future header height change needs updating in four places that currently share the `80px`/`h-20` value by convention rather than a token: the header's own height, `main`'s `pt-20`, and both pages' sticky `top-20` (baked into `TwoColumnLayout`) / `#projects` `scroll-mt-24`
6. The 752px project-grid breakpoint is a literal Tailwind class string (`PROJECT_GRID` in `app/page.tsx`) rather than a token, since Tailwind's class scanner can't see a computed value — if the shared 32px gap or the 360px minimum card width ever change, that string has to be hand-edited to match
7. About's "Outside of work" section needs real photos for gaming, singing/recording, the dog, and the AI game-building — none were supplied or existed in `public/images/` this session, so the section currently ships text-only; once supplied, each of the four items has an obvious slot for a small image
8. `TwoColumnLayout`'s `stickyGate` variants (`640px`/`760px`/`880px`) are calibrated for each page's *current* sidebar content specifically — a future sidebar or case-study-section-count change on any page should re-measure the actual rendered height (see the calibration method in this file's changelog) rather than assuming the existing threshold still has enough margin
9. Get a real Nokia logo asset (SVG or a high-resolution PNG) — `nokia-cover.webp` is currently built from a ~70×23px crop of an existing screenshot, the only Nokia wordmark instance anywhere in the repo, and it shows on close inspection; rebuild it once a proper source exists
10. Reconcile the Figma library with all of the above: `CaseStudyHero` should be retired there too (replaced in code by `CaseStudySidebar` + the right-column headline pattern), `CaseStudySidebar`/`CustomCursor` need adding, and the four case-study pages' Figma pattern needs rebuilding on the two-column shell to match code
11. `CustomCursor`'s coarse-pointer/reduced-motion bail-out and the window-leave/re-enter snap behavior were verified by code review and by exercising the mouse-driven paths in-browser; no touch device or OS-level reduced-motion toggle was available this session to test those specific branches live
